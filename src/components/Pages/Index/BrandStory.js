import React from "react";
import Style from "./brandstory.module.scss";

const BrandStory = props => {
    return (
        <div>
            {props.children}
        </div>
    )
};

export default BrandStory;

