// export default function removeOrderItem(state, id, category) {
//     let item = [...state.products].find(item => {});
//     return {
//         ...state,
//         item
//     }
// }


import db from "../mock-data/database"

export default function (state = db, action) {
    return state
}